# Backend Production Environment Variables
# These are required for the backend server

# Node Environment (production)
NODE_ENV=production

# Server Port
PORT=5000

# JWT Secret for token signing (generated automatically by Railway/Render if not provided)
# Generate a secure random string: `openssl rand -base64 32`
JWT_SECRET=your-secure-jwt-secret-here

# Database Configuration
# Option 1: SQLite (default, file-based)
DATABASE_PATH=/var/lib/spendsmart/app.db

# Option 2: PostgreSQL (uncomment to use PostgreSQL)
# DATABASE_URL=postgresql://user:password@host:port/database

# Optional: Plaid Configuration (for bank sync)
# PLAID_CLIENT_ID=your_client_id
# PLAID_SECRET=your_secret
# PLAID_ENV=production

# Optional: Gemini API Configuration (for AI features)
# GEMINI_API_KEY=your_gemini_api_key

# CORS Configuration (production domain)
CORS_ORIGIN=https://your-frontend-domain.com

# Optional: Enable debug logging
DEBUG=false
